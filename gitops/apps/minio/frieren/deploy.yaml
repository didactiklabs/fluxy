apiVersion: apps/v1
kind: Deployment
metadata:
  name: minio-frieren
spec:
  selector:
    matchLabels:
      app: minio-frieren
  template:
    metadata:
      labels:
        app: minio-frieren
    spec:
      containers:
      - name: minio
        image: minio/minio:RELEASE.2024-09-22T00-33-43Z.fips
        args:
        - server
        - /storage
        - --console-address
        - ":9001"
        env:
        - name: MINIO_API_ROOT_ACCESS
          value: "off"
        ports:
        - name: api
          containerPort: 9000
        - name: console
          containerPort: 9001
        volumeMounts:
        - name: storage
          mountPath: "/storage"
      volumes:
      - name: storage
        persistentVolumeClaim:
          claimName: minio-frieren
